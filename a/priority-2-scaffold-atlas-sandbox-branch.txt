# PRIORITY 2 — Scaffold feature/atlas-sandbox-mock-ticketing branch, items 1–7 only
# (Qoder Expert Mode). Run this AFTER Priority 1 is fully verified and green. This branch is
# NOT for tomorrow's Daytona submission — main stays on the working Sandbox Search→Verify
# path. This is groundwork for the Alibaba x Atlas submission, to be finished only once
# Atlas confirms ATRIP ticketing activation, whenever that happens.

## Branch setup

1. Confirm the working tree is clean and all Priority 1 fixes are committed on the current
   branch first. Do not scaffold this branch on top of uncommitted debug fixes.
2. Create and switch to a new branch: `feature/atlas-sandbox-mock-ticketing`, based off the
   current main/working branch.
3. Do not push this branch anywhere. Local only.

## Implement exactly items 1–7 from the approval-gate sequence (Section 22 of the spec) —
## these are the items that do NOT depend on any BLOCKED item, so they can be built safely
## without a live Atlas order/pay call ever executing.

1. **`.env.example` additions** — add `ATLAS_ENVIRONMENT=sandbox`,
   `ATLAS_SANDBOX_WRITES_ENABLED=false`,
   `ATLAS_SANDBOX_BASE_URL=https://sandbox.atriptech.com` as documented placeholder names
   only. Add the corresponding `vite.config.ts` define for a compile-time
   `__ATLAS_SANDBOX_WRITES__` boolean UI switch (secondary defense layer), and declare it in
   `vite-env.d.ts`.

2. **`core/simulation/sandbox-order-states.ts`** — the pure state machine only, no I/O:
   states `hidden → opt-in → order-review → order-submitting → order-created-unpaid →
   payment-review → payment-submitting → pay-accepted → status-polling →
   ticketed-simulated/cancelled`, plus error states `gate-rejected(403)`,
   `cli-error(502,code)`, `unknown-create`, `unknown-pay`, `safely-stopped`. Implement
   `canTransition(from, event)` as a pure function. No transition from an unknown-outcome
   state back to submitting without a fresh confirmation token. Write full unit tests for
   every transition in the table, including illegal-transition rejections.

3. **`server/atlas-proxy.mjs` — additive-only skeleton** — export the 5 existing private
   helpers (`execCli`, `sanitizeResponse`, `sanitizeError`, `sendJson`, `readBody`) so the new
   module can reuse them, and insert a `SANDBOX_ROUTES` exact-set dispatch stub between the
   existing `BLOCKED_PATHS` check and the `ALLOWED_ROUTES` check. The stub only recognizes the
   five `/api/atlas/sandbox/*` paths and currently returns 501 not-yet-implemented for all of
   them (real handlers come in item 4, which is separate and gated). This must be a maximum
   20-line additive diff to this file — do not restructure it.

## STOP HERE for items 4–7. Do the following instead of full implementation:

Items 4 (`server/atlas-sandbox-writes.mjs` full implementation), 5 (client/types/adapter), 6
(`SandboxOrderPanel.tsx` + App.tsx mount), and 7 (the gate-test file) all depend on exact CLI
response field names and the numeric status-code mapping, both marked `BLOCKED pending first
supervised rehearsal` in the spec. Building these now risks locking in guessed field names
that silently break later. Instead:

- Create each of these files as **empty scaffolds with a header comment** stating exactly
  what's blocked and citing the spec section, plus the full TypeScript interface/type
  definitions from the spec (these don't depend on the blocked items — only the runtime
  field-extraction logic does).
- Add a single `TODO(ATLAS_ACTIVATION_BLOCKED)` marker at each point where real field
  extraction would go, with the spec's defensive-extraction fallback chain written as a
  comment so it's ready to uncomment once activation clears.
- Do NOT guess at exact field names or numeric code semantics. Do NOT enable the kill switch
  in any config. Do NOT wire the panel into App.tsx yet.

## Constraints

- `ATLAS_SANDBOX_WRITES_ENABLED` must resolve to `false` everywhere by default — verify this
  with a grep after scaffolding.
- No live network call to `sandbox.atriptech.com` may occur from anything built in this pass.
- Do not touch `.env.local`, `core/safety-gates.ts`, `core/simulation/ticketing.ts`,
  `scripts/atlas-orchestrator.mjs`, `RecoveryPlanAnimation.tsx`, `AlternativesPanel.tsx`,
  `SafetyNotice.tsx`, `DecisionPanel.tsx`, any existing smoke-test file, or any
  video/presentation asset.
- Run `npm run typecheck` and the full existing regression suite at the end — everything must
  stay green, since items 1–3 touch the security-critical proxy file.
- No commit, no push. Leave changes staged/unstaged on the branch for manual review.

## Final report

1. Branch created and confirmed local-only (not pushed).
2. Files created/modified for items 1–3, with the exact diff size for `atlas-proxy.mjs`.
3. Sandbox-order-states.ts test results (all transitions, illegal transitions rejected).
4. Scaffold files created for items 4–7, each with their BLOCKED marker and spec citation.
5. Full regression + typecheck results on the new branch.
6. Explicit confirmation the kill switch defaults to false and no live call occurred.

End with:
```text
BRANCH_CREATED = <feature/atlas-sandbox-mock-ticketing, local-only: YES/NO>
ITEMS_1_3_STATUS = <IMPLEMENTED | BLOCKED: reason>
STATE_MACHINE_TESTS = <ALL_PASS | FAILURES: list>
ITEMS_4_7_SCAFFOLD_STATUS = <SCAFFOLDED_WITH_TODO_MARKERS | reason if not>
KILL_SWITCH_DEFAULT_FALSE_CONFIRMED = <YES | NO>
NO_LIVE_CALL_CONFIRMED = <YES | NO>
REGRESSION_STATUS = <ALL_PASS | FAILURES: list>
```
