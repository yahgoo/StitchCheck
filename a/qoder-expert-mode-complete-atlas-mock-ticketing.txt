# StitchCheck — Complete Atlas Mock Ticketing on feature/atlas-sandbox-mock-ticketing (Qoder
# Expert Mode). Target: finish items 4–7 fully (not just scaffolds) on this branch, ready to
# merge on Aug 30 for the Alibaba x Atlas hackathon submission. This branch is NOT for
# tomorrow's Daytona HackSprint demo — that runs on the current safe master branch state.
# Execute now, in order.

## Context — current branch state (do not disrupt this)

Branch `feature/atlas-sandbox-mock-ticketing` is based off baseline commit `8c910dc` on master,
which captures tonight's verified state:
- `DATA_MODE=offline` by default (P0 fix — no accidental live Atlas calls on default launch).
- One real, live Daytona sandbox cycle wired into the recovery animation
  (`daytona-risk-worker-v1`, risk band medium, score 54).
- One real, live Nosana job completed (`8CfUkxFgZnPpC5kxiphD1kozwiJeLYBC4KB33bKPAEp1`,
  `evidenceSource: "nosana-evidence"`, `fallbackUsed: false`).
- Historical Atlas Sandbox Search→Verify evidence (20 offers, `PRICE_CONFIRMATION_REQUIRED`,
  no write) correctly labeled as historical, not fresh.
- 8 docs reconciled, submission manifest regenerated, deck exported to PPTX with no
  "synthetic" wording.
- `SAFE_FOR_TOMORROW_DEMO = YES`.

**Do not modify, weaken, or regress any of the above in this branch.** This branch adds
ticketing scaffolding in parallel — it must not touch the live Daytona/Nosana wiring, the
DATA_MODE default, or any of the reconciled docs/manifest/deck.

## Still-blocked item (unchanged since last check)

**ATRIP ticketing activation** remains a human-action item in Atlas's own ATRIP workspace,
external to this codebase. Exact CLI order-create response field names and the numeric
status-code mapping (`0/1/2/-3`) remain unconfirmed pending a first supervised rehearsal that
can only happen after activation. If you have information that activation has since cleared,
STOP and report that first — it changes what's safe to build in this pass.

## Build exactly items 4–7 fully (previously scaffolded only — now complete them)

### Item 4 — `server/atlas-sandbox-writes.mjs` full implementation
Implement the full write handler module with:
- Kill switch (`ATLAS_SANDBOX_WRITES_ENABLED`), environment gate, production-write exclusion,
  sandbox URL gate, credential gate, route gate, input gate — all seven gates evaluated in
  order, any failure returns 403/401/400/409 with a machine-readable error code.
- Token store (single-use 120s confirmation tokens, operation-bound, lazy expiry sweep,
  capped at 100 entries with FIFO eviction, issuance rate-limited to 10/min).
- Idempotency store (Map key→state, lazy expiry, 30min TTL, same key in-flight → 409,
  completed → replay stored result, CLI never re-invoked).
- Paid index (Map orderNo→paid, payKey, suppresses any second pay per orderNo,
  confirmation-id marked consumed pre-flight, after a pay.do timeout there is never an
  automatic retry, a same-key follow-up reconciles via status instead).
- Synthetic passenger constant (server-owned, one fictional adult TESTTRAVELER, M,
  1990-01-01, nationality JP, document type PP, fixed synthetic document number, contact
  test@example.com 0000000000 — tamper-proof, any passenger/payment field in browser request
  → 400 rejection, payload built server-side from constant plus validated travelerid/
  passengertype sourced from Verify's data.travelers, delivered via stdin, never written to
  temp file, log redaction extends sanitizeResponse with PAN/CVV/document-number regexes).
- Five handlers: `capabilities`, `confirm-intent`, `order`, `pay`, `status` — each with exact
  request/response shapes from the spec, defensive field extraction (multi-key fallback chain
  for orderNo, confirmation-id, etc.), exact CLI invocations (order create with --booking-id
  and --passengers-stdin, order pay with --confirmation-id, order status with --order-no),
  zero retries on writes, single-flight status reads.
- Evidence writer (append-only JSONL to output/atlas-sandbox-evidence-UTC-date.jsonl, one
  line per operation, json envelope with version 1, correlationId, orderNo, operation,
  environment, route, idempotencyKeyHash, providerResponseCode, outcome, orderStatusCode,
  terminal, latencyMs, gateEvaluation, timestamp, noRealBooking, noRealCharge,
  noAirlineTicketIssued — never recorded: client id/secret, authorization headers,
  confirmation tokens/ids, full PAN/CVV, document numbers, passenger stdin payload, raw
  upstream responses in full, full CLI stderr truncated via sanitizeError to 500 chars).

### Item 5 — Client/types/adapter full implementation
- `app/src/atlas/types.ts`: full TypeScript interfaces for sandbox request/response envelope
  types (capabilities, confirm-intent, order, pay, status) — clone the existing atlasVerify
  pattern.
- `app/src/atlas/client.ts`: five new functions (capabilities, confirmIntent, order, pay,
  status) — clone the existing atlasSearch/atlasVerify fetch pattern, typed responses, no
  credential reference, no sandbox.atriptech.com URL in browser.
- `app/src/atlas/adapter.ts`: extend mapVerifyResponse to pass through bookingId/travelers
  additive fields, extend VerifySummary with bookingId?, travelers? passthrough.

### Item 6 — `SandboxOrderPanel.tsx` + App.tsx mount
- `app/src/components/SandboxOrderPanel.tsx`: new reducer-driven component implementing the
  full state machine from Section 5 (hidden → opt-in → order-review → order-submitting →
  order-created-unpaid → payment-review → payment-submitting → pay-accepted → status-polling
  → ticketed-simulated/cancelled, plus error states gate-rejected(403), cli-error(502,code),
  unknown-create, unknown-pay, safely-stopped).
- Pure functions in new file `core/simulation/sandbox-order-states.ts` (already implemented
  in baseline — reuse it, don't duplicate).
- Conditional rendering: panel renders only when ATLASSANDBOXWRITES=true AND DATA_MODE=live
  AND capabilities is true AND Verify succeeded with bookingId AND user has explicitly
  opted-in via acknowledgement checkbox — else null.
- `app/src/App.tsx`: mount the panel on the options screen (only when Verify succeeded with
  bookingId AND both switches are on — default flow byte-for-byte unchanged), extend
  handleRestart to reset sandbox state (clear sandbox state, abort polling), everything else
  verbatim.

### Item 7 — Smoke test file `smoke-tests/atlas/sandbox-write-gate-tests.mjs`
Implement full test suite covering:
- Unit/gates: kill switch false → all write routes 403, ATLAS_ENVIRONMENT!=sandbox → 403,
  ATLAS_WRITES_ENABLED=true refused, non-sandbox base URL → 403, regression re-assertion of
  default-deny and the 7 BLOCKED_PATHS (search/verify unchanged across every env combination).
- Tokens: single-use, 120s expiry, operation-bindingRef binding, cross-operation misuse
  rejected, replay rejected.
- Idempotency: same key in-flight → 409, completed → replay assert CLI not re-invoked,
  duplicate pay suppressed, N retries invoke the pay CLI exactly once, key-expiry handling.
- Inputs: browser-supplied passengers/card/name → 400, oversized travelerid → 400.
- Integration: mocked CLI envelopes full Search→Verify→Order→Pay→Status success chain,
  PRICE_CONFIRMATION_REQUIRED, OFFER_EXPIRED, invalid bookingId → order creation failure,
  PAYMENT_BALANCE_CHECK_REQUIRED → payment-blocked (never re-pay), 3DS-style rejection,
  pay timeout → poll-only (never re-pay), duplicate pay, polling timeout, ORDER_CANCELLED,
  malformed upstream response, upstream config error.
- Zero-retry assertion: capture CLI invocation counts (order/pay → 1 per request).
- Evidence redaction assertion: records contain no URL/email/Bearers/k-PAN/passenger payload.
- State-machine unit tests: sandbox-order-states.ts transition table, terminal codes, unknown
  numeric handling.
- Browser verification: flag off → panel invisible, routes 403; flag on → explicit opt-in,
  Order and Pay each require separate confirmation, no write request fires before
  confirmation, network-panel check, no real-data input controls, Sandbox copy present in
  every write state, refresh recovery, double-click prevention, terminal status display,
  no bare "production" wording.

## Constraints

- `ATLAS_SANDBOX_WRITES_ENABLED` must resolve to `false` everywhere by default — verify this
  with a grep after completing items 4–7.
- No live network call to `sandbox.atriptech.com` may occur from anything built in this pass.
- Do not touch `.env.local`, `core/safety-gates.ts`, `core/simulation/ticketing.ts`,
  `scripts/atlas-orchestrator.mjs`, `RecoveryPlanAnimation.tsx`, `AlternativesPanel.tsx`,
  `SafetyNotice.tsx`, `DecisionPanel.tsx`, any existing smoke-test file, or any
  video/presentation asset.
- Run `npm run typecheck` and the full existing regression suite at the end — everything must
  stay green, since item 4 touches the security-critical proxy file. Confirm the 188 Daytona
  offline tests and Nosana offline tests (post tonight's fixes) still pass unchanged.
- No commit, no push. Leave changes staged/unstaged on the branch for manual review before
  you decide to commit.

## Final report

1. Files created/modified for items 4–7, with exact diff sizes.
2. Confirmation the kill switch still defaults to false (grep results).
3. Full regression + typecheck results on the branch (must match baseline's green state).
4. Explicit confirmation no live call occurred (zero fetch/child_process in write module,
   zero sandbox.atriptech.com refs in app/src/).
5. Confirmation the panel is mounted but unreachable unless kill switch + live mode +
   capabilities + Verify + opt-in all pass (Order button has no handler until activation).

End with:
```text
ITEMS_4_7_STATUS = <FULLY_IMPLEMENTED | BLOCKED: reason>
KILL_SWITCH_DEFAULT_FALSE_CONFIRMED = <YES | NO>
NO_LIVE_CALL_CONFIRMED = <YES | NO>
REGRESSION_STATUS = <ALL_PASS, matches baseline | FAILURES: list>
PANEL_MOUNTED_UNREACHABLE_UNTIL_ACTIVATION = <YES | NO>
READY_TO_MERGE_AUG_30 = <NOT YET — pending ATRIP activation | blocked reason>
```