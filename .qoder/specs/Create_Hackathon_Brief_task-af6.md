## Create `docs/hackathon-brief.md`

### Step 1 -- Create the file

**File:** `/Users/kmsum/Downloads/Gemini Hackathon - Daytona HackSprint - Alibaba x Atlas Travel/docs/hackathon-brief.md`

The `docs/` directory does not exist yet; it will be created together with the file. The file content will reproduce the user's provided project context verbatim, with these sections in order:

1. Title: `# Build with Gemini Hackathon Brief`
2. **Deadline** -- Saturday, 22 August 2026; objective: select one idea from research, then build a working vertical slice today.
3. **Required Technology Stack** -- Gemini API, Atlas Flight Booking Sandbox, Nosana.
4. **Idea Status** -- no idea selected; no assumptions about any previous project, persona, target user, industry, or solution; selection only after research is supplied and evaluated.
5. **Build Constraints** -- all 11 constraints as provided (one official theme fit; essential visible roles for all three technologies in one user journey; Gemini structured output driving a real next step; genuine sandbox-backed Atlas results; Nosana workload consumed by the app; end-to-end vertical slice today; one user/one trigger/one decision/one outcome; no production credentials or real data; human confirmation before Atlas writes; loading/timeout/error/fallback states; repeatable demo over scope or polish).
6. **Hard Gate for Selecting an Idea** -- the three "removing X materially breaks" tests, plus the three evidence requirements (Gemini, Atlas, Nosana).
7. **Atlas Safety Notes** -- Sandbox only; search read-only; writes behind explicit human confirmation; fictional test data only; new search after environment switch.
8. **Official Technical References** -- the four URLs recorded verbatim (Gemini API docs, Atlas API Quick Start, Atlas Flight Booking Skill GitHub, Nosana Learn). These are recorded as references only; they will not be fetched.
9. **Evidence Rule** -- future capabilities confirmed by official documentation or a successful smoke test; otherwise listed as open questions.
10. **Required Outputs Before Code** -- the six downstream docs: `idea-scorecard.md`, `idea-context.md`, `user-stories.md`, `PRD.md`, `UAT.md`, `SPECS.md` (all under `docs/`).
11. **Stop Condition** -- no idea selection, no application code changes; confirm only that the file was created and nothing else was modified.

### Constraints honored

- Exactly one file created (`docs/hackathon-brief.md`).
- No application code modified.
- No skills, packages, plugins, or dependencies installed.
- No URLs browsed, no authentication, no API calls, no idea selection.

### Verification

- Read back `docs/hackathon-brief.md` to confirm content accuracy.
- Check the Git/source-control diff or Changed Files panel.
- Confirm that `docs/hackathon-brief.md` is the only created or modified file.
- Confirm that no application source files, dependency manifests, lockfiles,
  configuration files, credentials, or generated artifacts changed.
